# 移动端公文签收消息通知 PRD

版本：PRD-V1
日期：2026-08-20
时区：Asia/Shanghai

# 1. 背景与目标

教育局端已支持发布局校协同公文，并在签收进度页对未签收学校执行单校、批量及一键催办。学校端已支持查看教育局来文并签收，但缺少集中承载发布通知与催办通知的移动端消息入口。

本需求建设学校端独立“消息通知”页面。教育局正式发布公文后，系统向目标学校当前有效且具备查看及签收权限的用户生成“公文签收”消息；教育局有效催办后，将同一业务记录更新为“签收催办”消息。用户点击消息直接进入独立“教育局来文详情”，不经过公文管理列表或公文应用导航。学校完成签收后，消息继续保留在时间流中，后端内部处理状态更新为已办。

目标如下：

- 区分首次发布与签收催办，提升公文触达和签收效率。
- 通过业务唯一键合并重复催办，准确展示最近催办时间和累计催办次数。
- 建立“消息通知—独立来文详情—返回消息通知”的固定导航链路。
- 保持现有收件人计算、签收联动、事件幂等、失败重试和审计规则不变。
- 提供仅供原型评审使用的 PRD 规则入口，使字段来源、操作结果、状态流及后端生成机制可直接供研发拆解。

【假设】现有待办服务继续承载消息业务记录，并支持按业务唯一键创建、更新、关闭和查询；`todo_id`、`todo_status` 保持后端兼容，不作为消息页面的可见字段。

# 2. 用户与使用场景

## 2.1 用户角色

- 教育局公文发布人员：正式发布公文，触发新来文消息。
- 教育局公文管理人员：在签收进度页催办未签收学校，触发催办消息更新。
- 学校公文签收人员：在移动端接收消息、查看独立来文详情、确认签收及查看签收记录。
- 平台运维人员：监控事件消费、重试、告警和人工补偿。

## 2.2 使用场景

1. 教育局正式发布公文后，目标学校每名有效签收权限用户收到一条“公文签收”消息。
2. 教育局对未签收学校执行有效催办后，原业务记录更新为“签收催办”，不新增重复消息卡片。
3. 用户点击消息卡片或“查看详情”，直接进入与该消息关联的独立教育局来文详情。
4. 用户完成签收后可查看签收记录；返回消息页时原消息仍保留，后端内部状态统一转为已办。
5. 用户仅查看详情后返回，后端内部处理状态不变。

# 3. 需求范围

## In Scope

- 学校端首页评审入口名称为“消息通知（示例）”。
- 学校端独立消息通知页、单一时间流、两类消息模板和空状态。
- 消息卡片整体点击及“查看详情”进入独立教育局来文详情。
- 独立详情内的稿纸、正文、附件、确认签收和查看签收记录。
- 发布生成、催办合并、签收关闭、撤回关闭、权限失效隐藏等后端内部规则。
- 事件幂等、失败重试、乱序处理、权限校验、数据口径和埋点。
- 消息页右上角仅供评审使用的“PRD”规则入口及四类规则说明。
- `todo_id`、`todo_status`、`notification_type` 和既有服务事件的兼容使用。

## Out of Scope

- 不在消息页提供“全部、待办、已办、逾期”页签、状态标签或状态筛选。
- 不新增已读／未读、消息删除、消息分类筛选、消息保留期限和手动标记已办能力。
- 不将“消息通知（示例）”或“PRD”按钮作为正式产品入口发布。
- 不新增短信、电话、邮件、微信公众号或第三方即时通信渠道。
- 不调整教育局端发布、签收进度及催办入口。
- 不调整学校签收回执、签收意见和签收权限业务规则。
- 不建设消息模板或 PRD 规则的可视化编辑后台。

# 4. 功能需求列表

| 功能模块 | 功能点 | 需求描述 | 优先级（P0 / P1 / P2） | 备注说明 |
|---|---|---|---|---|
| 原型入口 | 消息通知（示例） | 仅学校端首页快捷区域展示小型描边按钮“消息通知（示例）”；点击进入独立消息通知页。 | P0 | 教育局端不展示；不进入正式产品 |
| 消息页面 | 页面结构 | 标题栏左侧为返回，中间为“消息通知”，右侧为小型“PRD”按钮；正文为按 `updated_at` 倒序排列的单一时间流。 | P0 | 不展示状态页签和状态标签 |
| 消息页面 | 空状态与错误 | 查询成功且结果为零时展示“暂无消息通知”；查询失败时展示错误反馈和重试入口，不得展示为空状态。 | P0 | 已加载数据在重试期间保留 |
| 发布通知 | 收件人计算 | 正式发布成功后，系统按接收学校查询账号有效、所属学校匹配且同时具备教育局来文查看和签收权限的用户。 | P0 | 草稿及发布失败不生成消息 |
| 发布通知 | 新来文消息 | 为每名收件人创建“公文签收”消息，主标题为“您有一份新的教育局来文待签收”，展示公文标题、发文单位和发布时间。 | P0 | 使用绿色文件图标 |
| 催办通知 | 催办更新 | 单校、批量或一键催办成功后，按业务唯一键更新已有记录为“签收催办”，不得新增重复记录。 | P0 | 支持新获得权限用户补建催办消息 |
| 催办通知 | 催办模板 | 主标题为“教育局提醒您尽快签收公文”，展示公文标题、最近催办时间和累计催办次数。 | P0 | 使用橙色提醒图标 |
| 消息合并 | 唯一键 | 业务唯一键固定为 `document_id + school_id + user_id`；服务端建立唯一约束。 | P0 | `todo_id` 为记录主键 |
| 消息卡片 | 查看详情 | 两类卡片底部均展示绿色“查看详情”和右箭头；点击卡片任意有效区域均携带 `document_id`、`school_id` 压入独立详情。 | P0 | 进入前执行服务端实时鉴权 |
| 独立详情 | 内容与操作 | 独立详情复用现有来文稿纸、正文、附件、确认签收和查看签收记录能力，不展示公文应用底部导航。 | P0 | 来源上下文固定为消息通知 |
| 独立详情 | 返回链路 | 详情返回仅执行当前 FlowStack `pop`，固定返回原消息通知页，不切换公文管理页签。 | P0 | 保持消息页滚动位置 |
| 签收联动 | 内部关闭 | 本校任一有权用户签收成功后，同一公文、学校下所有相关用户记录的内部 `todo_status` 更新为已办。 | P0 | 消息仍保留在时间流中 |
| 签收联动 | 查看不关闭 | 仅打开消息或详情不得改变内部处理状态。 | P0 | 阅读与办理分离 |
| 权限与撤回 | 可见性处理 | 用户停用、调离或失去权限后不得继续看到或处理消息；公文撤回后关闭内部记录并阻止签收。 | P0 | 历史审计记录保留 |
| 可靠性 | 幂等与重试 | 发布、催办、签收、撤回事件按 `event_id` 幂等消费；失败进入重试，超过阈值进入告警队列。 | P0 | 主业务结果不回滚 |
| PRD 规则层 | 规则入口 | 消息页右上角展示小型绿色描边“PRD”按钮，点击压入“消息通知 · PRD 规则”全屏说明层。 | P0 | 仅评审原型展示 |
| PRD 规则层 | 四类规则 | 规则层提供“页面规则、字段字典、状态流程、后端规则”四个页签，正文独立滚动且不执行数据修改。 | P0 | 不展示生产真实数据 |
| PRD 规则层 | 状态保持 | 打开、切换和关闭规则层不得重置消息页滚动位置或签收结果。 | P0 | 下层页面实例保持挂载 |

## 4.1 页面操作映射

| 页面控件 | 数据规则 | 点击处理 | 异常处理 |
|---|---|---|---|
| 消息页返回 | 不读取或修改业务数据 | 关闭消息通知页并返回学校端首页 | 返回动画期间禁止重复触发 |
| PRD | 读取原型内置结构化规则配置 | 压入“消息通知 · PRD 规则” | 配置缺失时展示“当前页面规则未配置” |
| 新来文消息卡片 | 读取 `notification_type=new_incoming` 及公文发布快照 | 携带 `document_id`、`school_id` 进入独立详情 | 无权、非接收学校、公文不存在或撤回时阻止进入并展示明确提示 |
| 催办消息卡片 | 读取 `notification_type=sign_reminder` 及有效催办聚合 | 携带相同跳转键进入独立详情；仅查看不修改内部状态 | 已签收时允许查看签收结果；撤回时禁止签收 |
| 查看详情 | 与所属卡片使用相同跳转参数 | 压入独立 `message-detail` 页面 | 跳转键缺失时禁止点击并提示关联公文不存在 |
| 详情返回 | 来源上下文固定为 `message_notification` | FlowStack `pop` 回原消息页 | 不得跳转公文管理列表或首页 |
| 确认签收 | 提交公文、学校、签收人和签收意见 | 生成学校级回执并关闭本校相关内部记录 | 重复签收返回已有回执；无权或撤回时拒绝提交 |
| 空状态 | 查询成功且结果为零 | 展示“暂无消息通知”，不执行操作 | 查询失败必须展示错误反馈和重试入口 |

## 4.2 字段字典

| 页面字段 | 接口字段 | 页面位置 | 来源服务 | 生成或转换规则 | 空值或异常处理 | 点击处理 |
|---|---|---|---|---|---|---|
| 消息时间 | `updated_at` | 卡片上方 | 待办服务 | 新来文取创建时间；催办取最近催办时间；按该值倒序 | 显示“时间异常”并告警 | 随卡片进入详情 |
| 模块名称 | `notification_type` | 卡片标题区 | 待办服务／移动端映射 | `new_incoming` 映射“公文签收”；`sign_reminder` 映射“签收催办” | 未知枚举显示“公文消息”并上报 | 不单独响应 |
| 图标 | `notification_type` | 卡片左侧 | 移动端映射 | 新来文为绿色文件图标；催办为橙色提醒图标 | 使用通用公文图标 | 不单独响应 |
| 主标题 | `notification_type` | 模块名称下方 | 移动端模板 | 按第 4 章固定文案映射 | 显示“您有一条公文签收消息” | 随卡片进入详情 |
| 公文标题 | `document_title` | 卡片正文 | 公文发布快照 | 正式发布时固化，只读展示 | 显示“未命名公文”并告警 | 按 `document_id` 查询详情 |
| 发文单位 | `issuer_name` | 新来文卡片正文 | 公文发布快照／组织服务 | 发布时固化名称，历史组织名称变化不回写 | 显示“发文单位未提供” | 不单独响应 |
| 发布时间 | `published_at` | 新来文卡片正文 | 公文服务 | 取正式发布成功时间，按 Asia/Shanghai 展示 | 缺失时不得生成消息 | 不单独响应 |
| 最近催办时间 | `latest_reminder_at` | 催办卡片正文 | 催办服务／待办聚合 | 取同一公文、学校最近有效催办成功时间 | 回退 `updated_at` 并记录异常 | 不单独响应 |
| 累计催办次数 | `reminder_count` | 催办卡片正文 | 待办服务 | 仅累计有效且未重复的催办事件 | 按 0 处理并触发校正 | 不单独响应 |
| 查看详情 | `document_id`,`school_id` | 卡片底部右侧 | 公文服务／接收快照 | 所有可访问消息固定展示绿色文字和右箭头 | 任一键缺失时隐藏入口并提示 | 压入独立详情 |
| 待办 ID | `todo_id` | 隐藏字段 | 待办服务 | 创建时生成且不可变 | 缺失记录不返回前端并告警 | 用于日志和埋点关联 |
| 内部处理状态 | `todo_status` | 隐藏字段 | 待办服务 | 取值为待办、逾期、已办或关闭；不参与页面展示或筛选 | 缺失时刷新并告警 | 无页面点击行为 |
| 公文 ID | `document_id` | 隐藏字段 | 公文服务 | 业务事件透传，前端不得生成 | 缺失时禁止跳转 | 详情主查询键 |
| 学校 ID | `school_id` | 隐藏字段 | 接收学校快照 | 发布时写入稳定 ID | 缺失时不得创建记录 | 与公文 ID 联合鉴权 |
| 用户 ID | `user_id` | 隐藏字段 | 用户与权限服务 | 按当前签收权限用户计算 | 缺失时不得创建记录 | 用户级查询和鉴权 |
| 通知类型 | `notification_type` | 隐藏业务字段 | 待办服务 | 首次发布为 `new_incoming`，有效催办后为 `sign_reminder` | 通用模板兜底并上报 | 决定模板、图标和埋点属性 |

# 5. 核心流程与交互说明

## 5.1 正式发布生成消息

1. 公文服务完成正式发布，写入公文、接收学校快照和发布时间。
2. 公文服务发送 `collab_document_published`，携带事件 ID、公文 ID、教育局 ID、学校 ID 列表和发布时间。
3. 待办服务按学校查询当前有效且同时具备查看及签收权限的用户。
4. 服务按 `document_id + school_id + user_id` 创建内部记录；唯一键已存在时幂等返回。
5. 学校用户在消息通知页看到“公文签收”消息，点击进入独立来文详情。

## 5.2 催办更新消息

1. 教育局用户在签收进度页选择未签收学校并确认催办。
2. 催办服务实时校验公文和签收状态，剔除已签收学校。
3. 催办成功后发送 `collab_sign_reminded`，携带事件 ID、公文 ID、学校 ID、催办时间和操作人。
4. 待办服务重新计算当前收件人，按业务唯一键更新通知类型、累计次数、最近催办时间和 `updated_at`。
5. 已存在记录更新原消息；新获得权限且不存在记录的用户创建催办消息；已失去权限的用户不再可见。

## 5.3 签收联动

1. 用户从消息通知页进入独立详情并提交签收。
2. 公文服务按公文 ID、学校 ID 幂等生成学校级签收回执。
3. 公文服务发送 `collab_school_signed`。
4. 待办服务将该公文、学校下所有待办或逾期记录更新为已办，记录签收人和签收时间。
5. 详情切换为“查看签收记录”；返回消息页后消息继续保留，不显示已办标签。

## 5.4 页面与导航交互

- 消息页按 `updated_at` 倒序展示单一时间流，不提供业务状态筛选。
- 两类消息通过图标、模块名称、主标题和业务字段区分；卡片不展示 `todo_status`。
- 卡片整体和“查看详情”使用同一跳转行为，进入独立详情前执行实时鉴权。
- 独立详情不挂载学校端公文管理页签及底部公文应用导航。
- 独立详情返回固定回原消息页；消息页返回固定回学校端首页。
- FlowStack 保留下层页面实例，详情或 PRD 规则层返回后恢复消息页滚动位置和本次会话签收结果。

## 5.5 PRD 规则层

1. 用户点击消息页右上角“PRD”，压入“消息通知 · PRD 规则”。
2. 默认展示“页面规则”，可切换“字段字典、状态流程、后端规则”。
3. 四个页签正文独立纵向滚动；长字段使用卡片化说明，不产生横向溢出。
4. 规则层只展示结构化定义和示例，不读取真实生产用户 ID、学校 ID、接口响应或公文正文。
5. 关闭规则层返回消息通知页，不执行签收、催办、编辑或数据修改。

## 5.6 内部处理状态转换

| 来源状态 | 触发事件 | 目标状态 | 前置条件 | 冲突处理 |
|---|---|---|---|---|
| 未生成 | `collab_document_published` | 待办 | 正式发布；学校在接收快照；用户有效且有权 | 相同业务唯一键幂等返回 |
| 待办／逾期 | `collab_sign_reminded` | 原状态 | 公文未撤回且学校未签收 | 更新催办模板和聚合值；重复事件不累计 |
| 待办 | 截止时间到达 | 逾期 | 无有效签收回执且公文未撤回 | 无截止时间时不产生逾期 |
| 待办／逾期 | `collab_school_signed` | 已办 | 已生成本校有效回执 | 同校用户记录批量关闭；已办优先级最高 |
| 待办／逾期 | `collab_document_withdrawn` | 关闭 | 公文撤回成功 | 保留关闭原因；历史访问提示撤回 |
| 任意状态 | 账号、学校或权限失效 | 隐藏 | 用户不再满足收件人条件 | 仅改变可见性，不删除审计记录 |

上述状态仅用于后端办理、审计和事件冲突处理，不等同于消息类型，也不在消息页展示或筛选。已办及关闭状态不得被迟到催办事件回退。

# 6. 异常场景与边界条件

- 草稿保存或发布失败：不得生成消息记录。
- 发布事件重复：按事件 ID 和业务唯一键幂等处理，不得生成重复记录。
- 目标学校无有效签收用户：发布保持成功，记录异常学校并进入告警队列。
- 发布后新增签收权限用户：不补发首次消息；后续有效催办时为该用户创建催办消息。
- 用户停用、调离或失权：消息不再可见；直接访问详情由服务端拒绝。
- 催办前已签收：不更新消息和次数，教育局端返回“该学校已签收”。
- 重复催办事件：最近催办时间和累计次数只更新一次。
- 公文撤回：内部记录关闭；历史访问提示“公文已撤回，无法查看或签收”。
- 学校不在接收快照：不得创建消息；直接访问返回无权限或记录不存在。
- 列表查询失败：展示错误反馈和重试入口；存在已加载数据时保留原数据。
- 详情加载失败：保留消息页实例，展示失败原因和重试入口。
- 用户仅查看详情：内部处理状态保持不变。
- 多人同时签收：只生成一条学校级有效回执；重复关闭事件幂等处理。
- 逾期后签收：内部状态更新为已办；消息仍保留且不展示状态标签。
- 待办服务不可用：发布、催办和签收主业务不回滚；事件进入重试队列。
- 超过重试阈值：进入告警队列，记录事件 ID、业务对象、失败原因、次数和最后失败时间。
- PRD 配置缺失：展示“当前页面规则未配置”，不得出现空白层。
- PRD 规则层重复打开：只保留一个实例；关闭后不得重置消息页状态。
- 长标题及长字段：允许纵向扩展和换行，不得产生横向溢出。

# 7. 数据口径与埋点需求

## 7.1 数据口径

- 业务唯一键：`document_id + school_id + user_id`，三项均为不可变业务 ID。
- 有效收件人：账号有效、当前所属学校匹配接收学校、同时具备教育局来文查看和签收权限。
- 消息查询范围：当前用户全部可见公文签收消息，包含内部已办记录，不按 `todo_status` 过滤。
- 消息排序：按 `updated_at` 倒序；同值时按 `todo_id` 倒序保证稳定排序。
- 通知类型：首次发布为 `new_incoming`；至少一次有效催办后更新为 `sign_reminder`。
- 累计催办次数：同一公文、学校的有效催办次数；重复事件、无效催办及签收后催办不计数。
- 内部处理状态：未签收且未超过截止时间为待办；未签收且超过截止时间为逾期；存在有效回执为已办；撤回为关闭。该字段不在页面展示或筛选。
- 消息保留：本期不提供删除和保留期限规则；已处理消息仍由默认查询返回。
- 时间字段由服务端生成，数据层保存统一标准时间，页面按 Asia/Shanghai 展示。

## 7.2 服务事件

| 事件名称 | 生产方 | 关键字段 | 消费结果 | 失败处理 |
|---|---|---|---|---|
| `collab_document_published` | 公文服务 | `event_id`,`document_id`,`bureau_id`,`school_ids`,`published_at` | 创建新来文内部记录 | 重试并告警，不回滚发布 |
| `collab_sign_reminded` | 催办服务 | `event_id`,`document_id`,`school_id`,`operator_id`,`reminded_at` | 更新或创建催办记录 | 重试并告警，不重复累计 |
| `collab_school_signed` | 公文服务 | `event_id`,`document_id`,`school_id`,`signer_id`,`signed_at` | 批量更新内部状态为已办 | 重试并以回执为最终事实 |
| `collab_document_withdrawn` | 公文服务 | `event_id`,`document_id`,`withdrawn_at` | 关闭未完成记录 | 重试并阻止后续催办回退 |

## 7.3 【假设】接口契约

- 消息列表查询入参包含 `biz_type=collab_sign`、`page`、`page_size`，不传 `todo_status`；返回第 4.2 节字段并按 `updated_at` 倒序。
- 独立详情查询使用 `document_id`、`school_id` 和 `source_context=message_notification`。
- 服务端实时校验用户所属学校、接收快照、查看权限和签收权限，前端参数不得替代鉴权。
- 接口 URL、分页结构和业务错误码以服务端联调文档为准，原型 PRD 规则层不固定正式地址。
- 事件以 `event_id` 消费幂等，记录以业务唯一键建立唯一约束。
- 乱序事件以有效签收回执和撤回结果为最终事实；已办或关闭不得被迟到催办回退。

## 7.4 埋点需求

| 事件名称 | 触发时机 | 关键属性 |
|---|---|---|
| `mobile_message_example_entry_click` | 点击“消息通知（示例）” | `portal_type`,`user_role` |
| `mobile_document_message_list_view` | 消息列表加载成功 | `user_id`,`school_id`,`result_count` |
| `mobile_document_message_card_view` | 消息卡片进入可视区 | `todo_id`,`notification_type`,`document_id`,`todo_status`,`reminder_count` |
| `mobile_document_message_click` | 点击卡片或“查看详情” | `todo_id`,`notification_type`,`document_id`,`school_id`,`todo_status`,`reminder_count`,`click_area` |
| `mobile_document_message_detail_result` | 独立详情加载或返回 | `todo_id`,`document_id`,`source_context`,`result`,`error_code` |
| `mobile_message_prd_entry_click` | 点击消息页“PRD” | `page_name`,`portal_type`,`user_role` |
| `mobile_message_prd_tab_click` | 切换规则页签 | `page_name`,`rule_tab` |
| `mobile_message_prd_close` | 关闭规则层 | `page_name`,`last_rule_tab`,`view_duration` |
| `document_todo_created` | 发布事件处理完成 | `event_id`,`document_id`,`school_id`,`recipient_count`,`result`,`error_code` |
| `document_todo_reminded` | 催办事件处理完成 | `event_id`,`document_id`,`school_id`,`recipient_count`,`reminder_count`,`result`,`error_code` |
| `document_todo_closed_by_sign` | 签收事件关闭记录完成 | `event_id`,`document_id`,`school_id`,`closed_count`,`result`,`error_code` |
| `document_todo_retry` | 事件重试 | `event_id`,`event_type`,`retry_count`,`error_code`,`result` |

埋点中的 `todo_status` 仅用于内部处理分析，不作为页面状态。PRD 规则层埋点不得采集规则正文、真实生产用户 ID、学校 ID、接口响应或公文正文。

# 8. 风险、依赖与限制

- 依赖公文服务在发布、签收和撤回成功后可靠发送事件。
- 依赖催办服务仅对实时未签收学校产生有效事件。
- 依赖组织与权限服务返回稳定的学校 ID、用户 ID 及实时权限结果。
- 依赖待办服务支持业务唯一键、模板更新、批量关闭、全量可见消息查询和内部状态维护。
- 多服务事件存在延迟和乱序风险，必须以有效签收回执及撤回结果作为最终事实。
- 签收权限用户数量较多时存在批量写入压力，服务端须分批处理并保持幂等。
- 待办服务失败不阻断公文主流程，消息短时延迟通过重试、告警和人工补偿控制。
- 【假设】公文提供签收截止时间；未提供时不产生内部逾期状态，且不影响消息页面展示。
- 原型使用前端模拟数据，不包含真实权限服务、事件队列、服务端持久化和跨用户同步。
- 原型内 `.todo-*` 样式类或 `todo_*` 字段属于内部兼容实现，不构成用户侧页面命名。

# 9. 验收标准

1. 学校端首页仅展示“消息通知（示例）”，教育局端不展示该入口。
2. 点击入口进入标题为“消息通知”的独立页面，标题栏包含返回和小型“PRD”按钮。
3. 消息页不展示“全部、待办、已办、逾期”页签及任何卡片状态标签。
4. 两类消息按 `updated_at` 倒序展示，视觉、模块名称、主标题和业务字段符合第 4 章。
5. 两类卡片底部均展示绿色“查看详情”和右箭头；点击卡片有效区域均进入对应独立详情。
6. 独立详情未经过公文管理列表，不展示公文应用底部导航，且展示的公文与 `document_id` 一致。
7. 独立详情返回固定回原消息通知页；消息通知页返回固定回学校端首页。
8. 详情返回后恢复消息页原滚动位置；PRD 规则层关闭后同样保持滚动位置和签收结果。
9. 独立详情支持稿纸、正文、附件、确认签收和查看签收记录。
10. 仅查看详情不改变内部处理状态；签收成功后内部状态更新为已办，消息仍保留且不显示状态标签。
11. 默认列表查询当前用户全部可见消息，不传 `todo_status`，内部已办记录仍返回。
12. 查询成功且无数据时展示“暂无消息通知”；查询失败时展示错误反馈和重试入口。
13. 教育局正式发布后，目标学校所有有效签收用户各获得一条新来文消息；草稿、发布失败及非接收学校不生成记录。
14. 有效催办只更新同一业务键记录的通知类型、最近催办时间和累计次数，不生成重复卡片。
15. 重复发布、催办、签收和撤回事件不得产生重复记录、重复计数或状态回退。
16. 本校任一有权用户签收后，同校相关用户记录的内部状态统一更新为已办。
17. 用户停用、调离或失权后不得继续查看或处理消息；直接访问详情由服务端实时鉴权拒绝。
18. 公文撤回后内部记录关闭，访问历史消息时展示明确撤回提示并禁止签收。
19. 待办服务失败不回滚发布、催办或签收主业务；失败事件可重试并在超过阈值后告警。
20. PRD 规则层标题为“消息通知 · PRD 规则”，四个页签均可切换并独立滚动。
21. 页面规则覆盖返回、PRD、两类卡片、查看详情、独立详情返回、确认签收和空状态。
22. 字段字典覆盖全部可见字段和隐藏字段，并逐项说明位置、接口字段、来源、生成规则、空值处理和点击行为。
23. `todo_status` 在原型规则层和正式 PRD 中均标记为隐藏内部字段，不作为消息类型、页面字段或筛选条件。
24. 状态流程覆盖发布、催办、逾期、签收、撤回和权限失效，并明确已办及关闭不得被迟到事件回退。
25. 后端规则覆盖事件生产方、关键字段、唯一键、收件人计算、幂等、重试、乱序、鉴权、详情参数和埋点。
26. iPhone 与 Pixel 10 下标题栏、卡片、长标题、“查看详情”、独立详情、签收弹层、规则页签及长内容无遮挡且无横向溢出。
27. 运行时完整性检查、TypeScript 构建、移动运行时测试、Sites 测试及浏览器控制台检查全部通过。
28. 现有教育局发布、签收进度、催办，以及学校端公文列表、详情和签收流程保持可用。
