# 问卷调研首页组件 PRD

## 1. 背景与目标

学校云平台与教育局云平台支持用户按角色在首页添加应用组件。问卷调研缺少首页聚合入口，用户需进入应用后才能查看待办及调研进度。

本需求新增“问卷调研”首页组件，覆盖学校端 PC、教育局端 PC、学校端移动端、教育局端移动端。PC 端用于查看统计概览和重点问卷并进入对应业务页面；移动端仅展示一个角色相关的核心统计值，组件整体只读，降低移动首页信息密度。

【假设】问卷调研应用已具备问卷填写、管理、发布、回收统计、结果查看及权限控制能力；本需求不改造应用内业务流程。

【假设】学校移动端核心统计固定为“待填写问卷”，教育局移动端核心统计固定为“待提交学校”。该口径不支持用户自定义。

## 2. 用户与使用场景

- 学校普通教职工：在 PC 首页查看本人待填写问卷并进入填写页；在移动首页快速确认待填写问卷数量。
- 学校问卷负责人、学校管理员：在 PC 首页查看待发布、进行中问卷及回收进度。
- 教育局普通工作人员：在 PC 首页查看本人待填写问卷并进入填写页。
- 教育局问卷负责人、教育局管理员：在 PC 首页查看跨校调研、学校覆盖及回收进度；在移动首页快速确认待提交学校数量。
- PC 用户：通过统计项、问卷条目和“查看更多”进入问卷调研应用对应页面。
- 移动端用户：仅查看当前组织对应的一个核心统计值，不在组件内执行点击、切换或业务跳转。

典型场景：用户在组件库添加“问卷调研”组件；系统根据当前组织类型和终端加载对应视图；PC 端用户继续处理问卷业务，移动端用户只读查看核心统计。

## 3. 需求范围

### In Scope

- 在学校端和教育局端的 PC、移动端组件库中新增“问卷调研”组件。
- PC 端展示三项统计、最多三条重点问卷及业务跳转入口。
- 学校移动端仅展示“待填写问卷”一个统计值。
- 教育局移动端仅展示“待提交学校”一个统计值。
- 移动端组件标题区、统计区及空白区域均不支持点击、跳转、按压反馈或内部切换。
- 支持权限控制、加载中、空数据、接口失败、组织切换及定时刷新。
- 新增组件曝光、加载结果及 PC 端点击埋点。

### Out of Scope

- 不新增问卷创建、编辑、发布、填写、催收、停止回收、统计分析及导出能力。
- 不调整问卷调研应用内既有页面结构和状态机。
- 移动端不展示问卷列表、进度条、截止时间、操作按钮、“查看更多”或页签。
- 移动端不支持统计项点击、组件标题点击、组件区域点击及业务跳转。
- 不支持用户自定义移动端统计指标。
- 不调整云平台组件库、布局管理、拖拽排序及个性化配置的通用机制。

## 4. 功能需求列表

| 功能模块 | 功能点 | 需求描述 | 优先级（P0 / P1 / P2） | 备注说明 |
|---|---|---|---|---|
| 组件配置 | 组件库上架 | 在学校端与教育局端的 PC、移动端组件库中新增“问卷调研”组件；名称和图标沿用问卷调研应用。 | P0 | 可见范围受应用授权控制 |
| 组件配置 | 添加与移除 | 用户可按平台既有规则添加、移除和排序组件；同一终端首页不得重复添加。 | P0 | PC 与移动端布局分别保存 |
| 权限控制 | 组件可见性 | 当前用户具备问卷调研应用访问权限时才可查看并添加组件；权限被回收后移除组件并清除缓存。 | P0 | 服务端必须校验权限 |
| 权限控制 | 数据分层 | 系统按当前租户、组织类型、用户角色及数据权限返回统计与列表数据。 | P0 | 禁止前端全量取数后过滤 |
| PC 通用展示 | 标题区 | 展示“问卷调研”和“查看更多”；点击“查看更多”进入问卷列表页。 | P0 | 携带来源参数 |
| 学校端 PC | 统计概览 | 展示“待填写、待发布、进行中”三项统计；统计值按当前用户权限范围计算。 | P0 | 统计项支持跳转 |
| 学校端 PC | 重点问卷 | 最多展示三条问卷，字段包含名称、状态、回收进度或发布方、截止时间。 | P0 | 按任务优先级排序 |
| 教育局端 PC | 统计概览 | 展示“进行中调研、覆盖学校、待提交学校”三项统计。 | P0 | 学校数按学校 ID 去重 |
| 教育局端 PC | 重点问卷 | 最多展示三条跨校问卷，字段包含名称、状态、已提交学校/目标学校、截止时间。 | P0 | 临近截止可展示待催收标识 |
| PC 端交互 | 业务跳转 | 点击统计项、问卷条目或“查看更多”进入对应列表、填写、编辑或统计详情页。 | P0 | 支持键盘聚焦和 Enter 激活 |
| 学校端移动端 | 单一统计 | 组件仅展示“待填写问卷”及其数量，不展示其他统计和问卷条目。 | P0 | 【假设】固定统计口径 |
| 教育局端移动端 | 单一统计 | 组件仅展示“待提交学校”及其数量，不展示其他统计和问卷条目。 | P0 | 【假设】固定统计口径 |
| 移动端交互 | 只读组件 | 移动端组件内不设置可点击元素，不绑定跳转、按压反馈、页签切换或长按操作。 | P0 | 首页组件管理操作不在组件内容区内 |
| 空状态 | 统计为零 | 移动端统计值为 0 时显示“0”，保持只读；PC 端无问卷时展示“暂无问卷任务”。 | P0 | 0 为有效业务值 |
| 异常状态 | 加载失败 | 接口失败时保留组件框架并展示“数据加载失败”；不得展示其他组织缓存数据。 | P0 | 单组件失败不阻塞首页 |
| 性能 | 首屏加载 | 组件数据异步加载；接口成功响应后 500 ms 内完成渲染。 | P1 | 加载时展示骨架屏 |

## 5. 核心流程与交互说明

### 5.1 组件添加流程

1. 用户进入首页组件库。
2. 系统仅向具备问卷调研应用权限的用户展示组件。
3. 用户将组件添加至当前终端首页。
4. 组件携带当前租户、组织类型、用户及终端类型请求聚合数据。
5. 系统按组织类型和终端渲染对应视图。

### 5.2 学校端 PC

- 顶部展示“待填写、待发布、进行中”三项统计。
- 下方最多展示三条重点问卷，按“已超期未提交、即将截止未提交、其他待填写、待发布、进行中”排序。
- 点击统计项进入带对应状态筛选的问卷列表；点击条目进入对应业务页；点击“查看更多”进入问卷列表。

### 5.3 教育局端 PC

- 顶部展示“进行中调研、覆盖学校、待提交学校”三项统计。
- 下方最多展示三条跨校问卷；进行中问卷展示“已提交学校/目标学校”。
- 点击统计项、条目及“查看更多”进入对应列表或统计详情页。

### 5.4 学校端移动端

- 组件使用白色圆角单列卡片。
- 标题区展示“问卷调研”，内容区仅展示“待填写问卷”一个统计值。
- 标题、统计值及整个组件内容区均不响应点击、触摸、长按或键盘激活，不展示箭头、按钮和按压态。

### 5.5 教育局端移动端

- 组件结构与学校端移动端一致。
- 内容区仅展示“待提交学校”一个统计值。
- 待提交学校数等于目标学校去重数减已提交学校去重数，计算结果最低为 0。
- 组件整体只读，不支持跳转和内部交互。

### 5.6 PC 端跳转映射

| 当前条目/入口 | 目标页面 | 携带条件 |
|---|---|---|
| 待填写问卷 | 问卷填写页 | 问卷 ID、任务 ID、来源=首页组件 |
| 待发布问卷 | 问卷编辑/发布页 | 问卷 ID、来源=首页组件 |
| 进行中问卷 | 问卷回收统计页 | 问卷 ID、来源=首页组件 |
| 已结束问卷 | 问卷结果详情页 | 问卷 ID、来源=首页组件 |
| 统计项 | 问卷列表页 | 组织类型、统计类型、来源=首页组件 |
| 查看更多 | 问卷列表页 | 组织类型、来源=首页组件 |

移动端不存在跳转映射。

## 6. 异常场景与边界条件

- 用户无应用权限：组件库不展示该组件；权限被回收后首页移除组件且停止请求业务数据。
- 用户切换学校、教育局或租户：立即清空原组织数据并重新加载，禁止短暂展示原组织统计值。
- 接口加载中：展示与最终结构一致的骨架屏，不以 0 代替未返回数据。
- 聚合接口失败或超时：仅当前组件展示失败态，不影响其他首页组件。
- 移动端统计字段缺失：显示“--”，不得按 0 处理。
- 学校端待填写任务数为 0：显示“0 待填写问卷”，组件保持只读。
- 教育局端目标学校数小于已提交学校数：待提交学校显示 0，并记录数据异常日志。
- 教育局端同一学校存在多名填写人：学校是否已提交按问卷既有学校完成规则判定，学校 ID 仅计一次。
- 问卷在组件加载后被删除或权限被回收：PC 端点击时由目标页二次鉴权；下次刷新移除对应数据。
- PC 条目数量超过三条：仅展示排序后的前三条，统计值仍按完整权限范围计算。
- PC 文本超长：名称单行省略，悬停展示完整名称；不得挤压状态和截止时间。
- 移动端组件不得因统计值为 0、缺失或加载失败而出现可点击重试入口；刷新由首页统一机制触发。

## 7. 数据口径与埋点需求

### 7.1 数据口径

- 待填写问卷：当前用户具有有效填写任务、尚未成功提交且问卷未删除的任务数；按任务 ID 去重。
- 待发布：当前用户有管理权限且状态为草稿或待发布的问卷数；按问卷 ID 去重。
- 进行中：当前用户有管理权限、已发布且处于有效回收期内的问卷数；按问卷 ID 去重。
- 覆盖学校：教育局当前权限范围内进行中调研的有效目标学校并集，按学校 ID 去重。
- 目标学校：教育局问卷有效发布范围内的学校数，按学校 ID 去重，不含已删除或停用学校。
- 已提交学校：目标学校中达到问卷既有学校完成规则的学校数，按学校 ID 去重。
- 待提交学校：`max(目标学校数 - 已提交学校数, 0)`；教育局移动端仅展示该统计。
- 数据范围：全部统计限定当前租户、当前组织及当前用户权限，不受 PC 列表展示条数限制。

### 7.2 埋点需求

| 事件名称 | 触发时机 | 核心属性 |
|---|---|---|
| `survey_widget_expose` | 组件进入可视区域且停留满 1 秒 | tenant_id、org_type、terminal、role_type、widget_position |
| `survey_widget_load_result` | 接口返回成功、空数据、失败或超时 | result、duration_ms、org_type、terminal、stat_value、item_count |
| `survey_widget_stat_click` | PC 用户点击统计项 | stat_type、stat_value、org_type、terminal=pc |
| `survey_widget_item_click` | PC 用户点击问卷条目 | survey_id、task_id、survey_status、item_rank、org_type、terminal=pc |
| `survey_widget_more_click` | PC 用户点击“查看更多” | org_type、terminal=pc |

移动端不设置点击埋点；不得通过透明热区、事件代理或父级容器上报点击事件。埋点不得上传问卷标题、答题内容、填写人姓名或其他敏感信息。

## 8. 风险、依赖与限制

- 依赖问卷调研应用提供按当前用户聚合的组件接口，并在服务端完成租户、组织及数据权限校验。
- 依赖问卷业务统一任务状态、问卷状态、学校完成规则及学校有效状态。
- 依赖 PC 端问卷页面支持携带问卷 ID、任务 ID、状态筛选及来源参数跳转。
- 移动端无业务入口，用户需通过移动工作台或问卷调研应用处理任务。
- 学校覆盖数据属于教育局管理数据，学校端及无管理权限用户不得获取。
- 前端缓存不得跨用户、跨租户复用；退出登录后必须清理。
- 本需求沿用平台现有主题、字号、圆角、间距及响应式规范。
- 【假设】PC 与移动端组件布局分别保存，PC 添加组件不自动同步至移动端。

## 9. 验收标准

1. 学校端和教育局端的 PC、移动端组件库均展示“问卷调研”，且仅对具备应用权限的用户可见。
2. 学校端 PC 展示“待填写、待发布、进行中”三项统计及最多三条重点问卷。
3. 教育局端 PC 展示“进行中调研、覆盖学校、待提交学校”三项统计及最多三条跨校问卷。
4. PC 端统计项、问卷条目及“查看更多”可进入正确业务页面并携带规定参数。
5. 学校移动端组件仅展示“待填写问卷”一个统计值，不展示其他统计、列表、箭头、按钮或页签。
6. 教育局移动端组件仅展示“待提交学校”一个统计值，不展示其他统计、列表、箭头、按钮或页签。
7. 两类移动端组件的标题、统计值及内容区域均不可点击，不产生跳转、按压反馈或点击埋点。
8. 移动端统计为 0 时显示 0，字段缺失时显示“--”，两种状态均保持只读。
9. 教育局移动端待提交学校数按 `max(目标学校数 - 已提交学校数, 0)` 计算，学校按 ID 去重。
10. 加载中展示骨架屏；接口失败时仅当前组件展示失败态，不影响首页其他组件。
11. 切换租户或组织时原组织数据立即清空，刷新后不出现跨租户、跨组织数据。
12. PC 列表超过三条时仅展示排序后的前三条，统计值仍等于完整权限范围内数据。
13. PC 可交互元素支持 Tab 聚焦和 Enter 激活，状态同时使用文本表达。
14. 第 7.2 节埋点按触发条件上报，移动端不存在点击事件，全部事件不包含敏感信息。
15. 问卷应用内创建、编辑、发布、填写、催收、结束、统计及导出流程不因本需求变化。
